// $('#new-note form').remove();
$('#new-note a').show();
$('div#notes').prepend(
  $("<%= j render @note %>").hide().fadeIn('slow')
);

$('#exampleModal').modal('hide');

bootoast.toast({
  message: 'Note created successfully!',
  position: 'top-left',
  type: 'success'
});
