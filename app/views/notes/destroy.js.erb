$('.delete').bind('ajax:success', function () {
  $(this).closest('.card').fadeOut();
});

bootoast.toast({
  message: 'Note deleted successfully!',
  position: 'top-right',
  type: 'success'
});
